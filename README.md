# TopAppBarDrawer
A draggable TopAppBar for navigation.

``Could be used``
 TopAppBarDrawer(
                        drawerContent = { },
                    ) {
                        Scaffold()
                    }
